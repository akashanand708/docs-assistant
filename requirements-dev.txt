# Development dependencies
ruff>=0.9.0
pre-commit>=3.0.0

# Evaluation / benchmarking
# ragas 0.2.15 requires a shim after install — see CLAUDE.md
ragas==0.2.15
langchain-google-vertexai>=1.0.0  # required by ragas shim
datasets>=2.0.0
